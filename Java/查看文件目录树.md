# 查看文件目录树 

```
windows + R ⇒ 输入 cmd ⇒ 进入 windows 命令行界面：

tree/?：命令提示；
tree：不输入任何参数，输出一棵目录树 
不显示文件，只显示目录；
tree/F：递归显示目录结构； 
显示目录，也显示文件；
tree /F，在给出项目文件说明时，会十分有用；
1. 重定向到文本文件
tree /f > info.txt 
tree > info.txt
```

